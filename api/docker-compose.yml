services:
  platform-insights-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: platform-insights-api
    ports:
      - "8000:8000"
    environment:
      APP_NAME: "Platform Insights API"
      APP_ENV: "local"
      APP_VERSION: "1.0.0"
      LOG_LEVEL: "INFO"